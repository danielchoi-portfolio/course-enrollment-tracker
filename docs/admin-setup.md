# Admin Setup Guide

<!-- TODO: This is styled like the system guides you wrote at Leidos — a
     step-by-step config walkthrough for someone setting this up from a
     blank org. Replace every step below with what you actually did, in
     the order you actually did it. Add screenshots where a click sequence
     is easy to get wrong. -->

This guide walks a Salesforce admin through configuring a blank Developer
org to run the Course Enrollment & Conversion Tracker from scratch.

## Prerequisites

- A Salesforce Developer Edition org ([free signup](https://developer.salesforce.com/signup))
- System Administrator profile access

## Step 1: Create the Custom Objects

<!-- TODO: exact path, e.g. "Setup → Object Manager → Create → Custom Object" -->

1. [First click/step]
2. [Next step]
3. [Next step]

## Step 2: Add Fields

<!-- TODO: reference docs/data-model.md so the two docs don't drift out of
     sync — link to it instead of repeating the full field list here. -->

See [`data-model.md`](data-model.md) for the full field reference. Add each
field listed there to its corresponding object.

## Step 3: Configure Page Layouts

<!-- TODO: which fields go where, and why, e.g. "Acquisition Channel is
     placed at the top of the Inquiry layout since it's the first thing a
     staff member records." -->

## Step 4: Set Up Reporting (optional)

<!-- TODO: if you build a conversion-by-channel report or dashboard,
     document the report type, filters, and grouping here. -->

## Troubleshooting

| Issue | Likely Cause | Fix |
|---|---|---|
| [Something that actually tripped you up while building this] | | |

<!-- TODO: this table is the most valuable part of the doc if you fill it
     in honestly — the real errors you hit while building are exactly what
     the next person needs. Don't leave it empty. -->
